# Copy to .env.local and fill in your own Supabase project to move off the mock data layer.
# The app runs fully on mock data (src/mock/data.ts) when these are unset.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
